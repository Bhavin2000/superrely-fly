app = "superrely-fly"
primary_region = "bom"

[build]
  dockerfile = "Dockerfile"

[mounts]
  source = "gun_data"
  destination = "/app/data"

# This is the most important part!
[deploy]
  strategy = "immediate"

[[services]]
  internal_port = 8765
  protocol = "tcp"
  # Simplified settings to avoid conflicts
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

  [[services.ports]]
    handlers = ["http"]
    port = 80

  [[services.ports]]
    handlers = ["tls", "http"]
    port = 443

[[vm]]
  memory = "512mb"
  cpu_kind = "shared"
  cpus = 1
